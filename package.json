{
  "name": "sst-notes",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "devDependencies": {},
  "dependencies": {
    "aws-cdk-lib": "2.7.0",
    "@serverless-stack/cli": "0.60.1",
    "@serverless-stack/resources": "0.60.1",
    "aws-sdk": "^2.1057.0",
    "uuid": "7.0.3"
  }
}
